# Historical MATLAB ABM source

These files preserve the pre-refactor M3AS MATLAB implementation and its
original examples for provenance. They are not the supported reproduction
entry point and intentionally retain the historical function and parameter
conventions.

Use the maintained implementation under [`matlab/`](../../matlab/README.md) for
new runs, both papers' ABM figure commands, structured output, and tests.
